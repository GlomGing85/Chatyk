# Файли, які НЕ потрібно завантажувати на GitHub
*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/app/build
/captures
.externalNativeBuild
.cxx
*.keystore

# Зібрані APK — їм місце в Releases, а не в коді
*.apk
*.aab

# Ключі підпису — НІКОЛИ не заливати в репозиторій!
*.jks
*.p12
*.pem
keystore.properties

# Kotlin compiler local state
.kotlin/
